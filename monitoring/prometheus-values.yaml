serverFiles:
  prometheus.yml:
    scrape_configs:
      - job_name: prometheus
        static_configs:
          - targets:
              - localhost

      - job_name: notes-app
        metrics_path: /metrics
        static_configs:
          - targets:
              - notes-app.devops-pet.svc.cluster.local:8000